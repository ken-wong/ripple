<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to t(:home), admin_root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="<%= 'active' if params[:controller] == 'admin/users' %>">
          <%= link_to User.model_name.human, admin_users_path %>
        </li>
        <li class="<%= 'active' if params[:controller] == 'admin/projects' %>">
          <%= link_to Project.model_name.human, admin_projects_path %>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="<%= 'active' if params[:controller] == 'admin/admins' %>">
          <%= link_to Admin.model_name.human, admin_admins_path %>
        </li>
        <li>
          <%= link_to t(:logout), admin_logout_path, method: :delete if admin_logged_in? %>
        </li>
        <li>
          <%= link_to t(:login), admin_login_path if !admin_logged_in? %>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="glyphicon glyphicon-user" aria-hidden="true">&nbsp;<%= current_admin.name if admin_logged_in? %></span></a>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
