<%= simple_form_for([:admin, @admin]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :password if @admin.new_record? %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
